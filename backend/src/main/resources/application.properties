spring.application.name=taskbuddy
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/taskbuddy}
server.port=${PORT:8080}
jwt.secret=${JWT_SECRET:taskbuddy-secret-key-must-be-at-least-256-bits-long-for-hs256}
jwt.expiration=${JWT_EXPIRATION:86400000}
app.frontend.url=${APP_FRONTEND_URL:http://localhost:5173}
